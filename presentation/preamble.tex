\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english,russian]{babel}

% Оформление
\usetheme{Pittsburgh}  % Тема
\usecolortheme{beaver} % Цветовая схема

% Переносы слов, как в ворде 
\usepackage[none]{hyphenat}
\sloppy

% Выравнивание по ширине
\usepackage{ragged2e}
\justifying

% Цвет ссылок
\hypersetup{colorlinks=true,urlcolor=blue,linkcolor=black}

% Горизонтальные скобки
\usepackage{amsmath} 

% Цвет item в description
\setbeamercolor*{item}{bg=black}

% Знак градуса
\usepackage{gensymb}

% Ориентация
\usepackage{pdflscape}

% Отступы
\usepackage{geometry}

% Bg текста
\usepackage{adjustbox}
\definecolor{mygrey}{HTML}{F3F3F3}
% Fg текста
\usepackage{xcolor}
\definecolor{myred}{HTML}{CE0808}

% Таблицы
\usepackage{multirow}
\usepackage{array} % Ширина столбца + выравнивание по середине

% Рисование
\usepackage{tikz}
\usetikzlibrary{chains,shapes.misc,arrows}

% Сноски со звёздочками
\renewcommand{\thefootnote}{\fnsymbol{footnote}}